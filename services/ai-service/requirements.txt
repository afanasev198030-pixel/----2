fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.5
pydantic-settings==2.7.1
structlog==24.4.0
httpx==0.28.1
pdfplumber==0.11.4
Pillow==11.1.0
python-multipart==0.0.20
PyYAML>=6.0

# LLM (direct OpenAI)
openai>=1.50.0,<2.0
tiktoken>=0.7.0,<1.0

# Excel parsing
openpyxl>=3.1.0
xlrd>=2.0.1

# DOCX parsing
python-docx>=1.1.0

# ChromaDB client + default embedding deps
chromadb-client>=1.0.0,<2.0
onnxruntime>=1.17.0
tokenizers>=0.15.0

# Эмбеддинги: ChromaDB default ONNX (all-MiniLM-L6-v2, ~90MB)
# sentence-transformers убран — PyTorch слишком тяжёлый для 8GB RAM

# DSPy — авто-оптимизация промптов, few-shot (не тянет PyTorch)
dspy>=2.5.0
crewai

# Phoenix observability — LLM трейсинг (dashboard на :6006)
arize-phoenix>=8.0.0
openinference-instrumentation-openai>=0.1.0
redis>=5.0.1

# Task Queue (ARQ + Redis) - for async AI processing to prevent OOM and timeouts
arq>=0.25.0
